Generate the PHP code to register a new Custom Post Type in WordPress.
- Singular Name: "{{singularName}}"
- Plural Name: "{{pluralName}}"
- Slug: "{{slug}}"
- Associated Taxonomies: {{taxonomies}}
The code should be a complete PHP file that registers the CPT using the 'init' action hook.
Provide the response as a single JSON object with one key: "php".
Ensure the JSON is valid.