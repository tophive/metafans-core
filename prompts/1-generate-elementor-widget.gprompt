# Task
Create a new Elementor widget for WordPress named {{widgetName}}.

# Requirements
- Generate full PHP class under `elementor/includes/widgets/`.
- Use namespace: `TophiveElementor\Widgets`.
- Include `register_controls()`, `render()`, and `get_style_depends()` if needed.
- Add default Elementor icon and category support.
- Use WordPress coding standards and escape all outputs.
- Prepare hooks for JS/CSS enqueueing.

# Output
PHP code only, properly formatted and documented.
