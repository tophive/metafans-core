# Task
Generate optimized JS for Elementor widget {{widgetName}}.

# Requirements
- Use ES6 modules.
- Include GSAP animation logic if applicable.
- Automatically initialize via Elementor’s frontend hook: `elementorFrontend.hooks.addAction('frontend/element_ready/widget-name.default', callback)`.
- Avoid global namespace pollution.
- Ensure cleanup on `elementorFrontend.hooks.doAction('frontend/element_unload')`.

# Output
Pure JS code only.
